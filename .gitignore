# build output
bin/
obj/
*.user

# whisper models (downloaded on first run — large binaries)
models/

# local settings / secrets (belt & braces — real secret storage is DPAPI)
*.local.json
.env

# IDE
.vs/
.vscode/
.idea/

# audio scratch (benchmark fixtures are committed deliberately, nothing else)
*.wav
!tests/fixtures/*.wav
!src/Outspoken.App/Assets/*.wav

# local bridge to planning vault (contains personal paths) - never public
CLAUDE.md

# fixture transcripts (personal content, local verify only)
tools/WhisperBenchmark/fixtures/*.txt

# Audio reference samples — local only, used to tune the cue synth; never committed
docs/audio-refs/

# Logo/brand design working files (SVG source, brainstorm exports, zips) — local only.
# The canonical mark lives in code (Overlay/BrandMark.cs) + the generated app.ico.
docs/logo-refs/
