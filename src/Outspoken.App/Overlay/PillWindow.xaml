<Window x:Class="Outspoken.App.Overlay.PillWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:overlay="clr-namespace:Outspoken.App.Overlay"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True" ShowActivated="False"
        ResizeMode="NoResize" SizeToContent="Manual" Focusable="False"
        IsHitTestVisible="False">
  <!-- Outer margin gives the drop shadow room so it isn't clipped by the window edge. -->
  <Grid Margin="16">
    <overlay:PillVisual x:Name="Visual" />
  </Grid>
</Window>
